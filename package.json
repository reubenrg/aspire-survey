{
  "name": "aspire-survey-deployment",
  "private": true,
  "scripts": {
    "build": "npm --prefix apps run build",
    "dev": "npm --prefix apps run dev",
    "deploy": "npx vercel@latest --prod"
  },
  "engines": {
    "node": ">=20.19"
  }
}
