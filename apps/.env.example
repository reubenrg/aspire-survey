# Supabase project that stores survey responses.
# Both values are safe to expose in the browser bundle — the anon key is a
# publishable key and writes are gated by row level security.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key
